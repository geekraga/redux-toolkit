import { apiUrl } from '../constants/apiConstant';

export default({
    languagePath: apiUrl.BASE + apiUrl.language,
    videoCategoriesPath: apiUrl.BASE + apiUrl.videoCategories,
    categoryVideoPath: apiUrl.BASE + apiUrl.videoList,
    propertyDetilsPath: apiUrl.BASE + apiUrl.propertyDetils,
    slideshowPath: apiUrl.BASE + apiUrl.slideshow,
});
